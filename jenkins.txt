pipeline {
    agent any

    environment {
        // Define environment variables for JDK and MAVEN_HOME if necessary
        JAVA_HOME = 'C:\Program Files\Java\jdk-21'
        MAVEN_HOME = 'C:\\apache-maven-3.8.1'
        PATH = "${JAVA_HOME}\\bin;${MAVEN_HOME}\\bin;${env.PATH}"
    }

    stages {
        stage('Checkout') {
            steps {
                // Checkout the source code from the repository
                git 'https://github.com/usamabutt78678-sudo/jenkins.git'
            }
        }

        stage('Build') {
            steps {
                // Compile the HelloWorld.java file using javac (if you're not using Maven)
                script {
                    bat 'javac helloworld.java'
                }
            }
        }

        stage('Test') {
            steps {
                // Run unit tests (if using Maven, this would be part of the build)
                script {
                    bat 'java HelloWorld'  // Run the compiled HelloWorld class
                }
            }
        }

        stage('Archive') {
            steps {
                // Archive the compiled class file for future reference
                archiveArtifacts artifacts: '**/*.class', allowEmptyArchive: true
            }
        }

        stage('Deploy') {
            steps {
                // Deploy step (optional, depending on your needs)
                echo 'Deployment step would go here.'
            }
        }
    }

    post {
        success {
            echo 'Build completed successfully!'
        }

        failure {
            echo 'Build failed.'
        }
    }
}

